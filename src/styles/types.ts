import type { CSSProperties } from 'react';

export interface AppStyleSet {
  container: CSSProperties;
  header: CSSProperties;
  title: CSSProperties;
  status: CSSProperties;
  card: CSSProperties;
  adBlock: CSSProperties;
  adText: CSSProperties;
  correctionWrap: CSSProperties;
  strikeWord: CSSProperties;
  correctionNote: CSSProperties;
  fallback: CSSProperties;
  gameLauncher: CSSProperties;
  startBtn: CSSProperties;
  stopBtn: CSSProperties;
}
